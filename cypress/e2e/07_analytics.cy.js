describe('Analytics modal' , ()=> {
    // starts with nothing
    // when transaction is added, number fields populate & graph appears
    // transaction delete, takes away from graph & number values
    // when all transactions are deleted, numbers are 0 & graph disappears
    // correct values & colors are rendered for each portion of cashflow: income, expenses, neg cashlow & overspent
    // correct values & colors are rendered for each portion of net wroth: debit accs, savings, CC total, & total
} )